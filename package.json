{
  "name": "company-dinner-checkin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "start": "node server/index.js",
    "deploy": "npm run install:all && npm run build:client && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
